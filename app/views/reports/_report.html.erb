<div id="<%= dom_id report %>">
  <p>
    <strong><%= Report.human_attribute_name(:created_at) %>:</strong>
    <%= l report.created_at, format: :created_at_for_report %>
  </p>

<!--  <p>-->
<!--    <strong><%#= Report.human_attribute_name(:user) %>:</strong>-->
    <%#= [User.find(report.user_id).name, User.find(report.user_id).email].find(&:present?) %>
<!--  </p>-->

  <p>
    <strong><%= Report.human_attribute_name(:user) %>:</strong>
    <%= [report.user.name, report.user.email].find(&:present?) %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:title) %>:</strong>
    <%= report.title %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:content) %>:</strong>
    <%= report.content %>
  </p>

</div>
