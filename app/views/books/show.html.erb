<h1><%= t('views.common.title_show', name: Book.model_name.human) %></h1>

<div class="show-item">
  <%= render @book %>
</div>

<div id="comments" class="index-items">
  <% if @comments.empty? %>
    コメントはありません😹
  <% else %>
    <% @comments.each do |comment| %>
      <div class="index-item">
        <%#= render comment %>
        <div id="<%= dom_id comment %>">
          <p>
            <strong><%= Comment.human_attribute_name(:created_at) %>:</strong>
            <%= comment.created_at %>
          </p>

          <p>
            <strong><%= Comment.human_attribute_name(:user_id) %>:</strong>
            <%= User.find(comment.user_id).email %>
          </p>

          <p>
            <strong><%= Comment.human_attribute_name(:body) %>:</strong>
            <%= comment.body %>
          </p>

          <p>
          </p>
          <%= link_to t('views.common.edit', name: Comment.model_name.human.downcase), edit_polymorphic_path(comment) if current_user.id == comment.user_id %>
          <%= button_to t('views.common.destroy', name: Comment.model_name.human.downcase), comment, method: :delete if current_user.id == comment.user_id %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

<%= render 'comments/comment_form', comment: @comment, commentable_id: @book.id, commentable_type: "Book", commentable: @book %>


<nav class="page-nav">
  <%= link_to t('views.common.edit', name: Book.model_name.human.downcase), edit_book_path(@book) %> |
  <%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human.downcase)), books_path %>

  <%= button_to t('views.common.destroy', name: Book.model_name.human.downcase), @book, method: :delete %>
</nav>
